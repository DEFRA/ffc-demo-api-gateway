environment: development
container:
  readOnlyRootFilesystem: false
  allowPrivilegeEscalation: true
  command: ["npx"]
  args: ["nodemon", "--inspect=0.0.0.0", "--ext", "js", "--legacy-watch", "app"]
replicaCount: 1
